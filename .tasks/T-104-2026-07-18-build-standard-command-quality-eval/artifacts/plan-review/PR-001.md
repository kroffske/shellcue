# Plan review PR-001

Profile: standard
Topology: self
Authority: gate
Base hash: `a20c46d21a885b83e391bdc9d3e53c63c6a4e75f27440384834994a8c8bcdfc9`
Verdict: ready_with_residual_risk

This is a self-review, not independent validation.

## Evidence reviewed

- T-104 task, draft, planning record, and REQ-005.
- The current Smart Bash v5 panel/evaluator and its catalog-dependent mirror
  helpers.
- The installed model-only ShellCue responses and accepted T-106 rollback
  evidence.
- Pre-code shape review in `artifacts/planning-locus-code-review.md`.

## Gate assessment

The plan correctly preserves the public/synthetic panel and metrics while
invalidating the old policy-assisted baseline. It separates standard-command
quality from T-108 history-conditioned evaluation, freezes identities and
thresholds before checkpoint comparison, and treats abstention separately from
contamination.

Implementation ownership is clear: evaluator/data code stays in Smart Bash;
ShellCue is invoked as the installed model-only production runtime. The first
deletion target is explicit, and no adapter may relabel or repair candidates.

No blocking ambiguity remains.

## Residual risk

The Smart Bash mirror and installed ShellCue runtime may diverge after the
catalog is removed because they are separate implementations. The task must
either prove byte-for-byte candidate parity under bound artifact/config hashes
or fail as incomparable; it may not heal the discrepancy. The alpha baseline
is also expected to fail quality thresholds, which is a valid measurement
result rather than task failure.
